VITE_API_BASE_URL=http://127.0.0.1:8010/api

# Optional: for a single-restaurant customer deploy, set this so "/" opens
# their public menu with zero clicks (no slug form).
# VITE_DEFAULT_SLUG=ember-rye
